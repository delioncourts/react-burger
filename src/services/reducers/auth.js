//авторизация

const defaultState = {
    name: '',
    email: '',
    isLoggedIn: false,
    isPasswordReset: false
};

export const authReducer = (state = defaultState, action) => {
    switch (action.type) {

    }
}